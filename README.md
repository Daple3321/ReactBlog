# ReactBlog

Backend API is written in C#

Frontend is in React

Uses Sqlite database and Entity Framework Core

Supports CRUD operations
* View all blogs ```GET /blogs/all```
* View specific blog ```GET /blogs/{blogId}```
* Delete blog ```DELTE /blogs/{blogId}```
* Update blog ```PUT /blogs/{blogId}```
